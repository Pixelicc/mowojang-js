name: Node.js CI (ESLint)

on:
  push:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Use PNPM Package Manager
        uses: pnpm/action-setup@v6
        with:
          version: 11
      - name: Node.js (26.x)
        uses: actions/setup-node@v6
        with:
          node-version: "26.x"
          cache: "pnpm"
      - name: Check code with ESLint
        run: pnpm lint:check
